{
  "name": "ctx-switch",
  "version": "0.1.0",
  "private": true,
  "description": "Context Switcher - AI-powered developer context capture and resumption tool",
  "workspaces": [
    "packages/shared",
    "packages/lambdas",
    "packages/cli",
    "packages/web",
    "packages/infra"
  ],
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "fast-check": "^4.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.1.0"
  }
}
